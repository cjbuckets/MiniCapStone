<!DOCTYPE html>
<html>
<head>
  <title>PillsApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <% flash.each do |name, message| %>
    <div class="alert alert-<%= name %> alert-dismissible" role="alert">
      <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
      <strong><%=name.upcase%></strong> <%= message %></div>
  <% end %>


  <nav class="navbar navbar-inverse"><
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Pillz</a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li class="active"><a href="/">Home <span class="sr-only">(current)</span>
          
      
        <% if user_signed_in? %>
        <li class="active"><a href="#"><%= current_user.email %><span class="sr-only">(current)</span></a></li>
         <% end %>
       
          <li><a href="/users/sign_in">Sign In</a></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Menu <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="/?sort=price&sort_order">Price $ - $$$</a></li>
              <li><a href="/?sort=price&sort_order=desc">Price $$$ - $</a></li>
              <li><a href="/pills/random">Random Product</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="/?discount=890000">Discount Items</a></li>
              <li role="separator" class="divider"></li>
              
            
              <% if user_signed_in? %>
                <li><%= link_to 'Sign Out', '/users/sign_out', method: :delete %></li>
                  <% else %>
                <li><%= link_to 'Sign In', '/users/sign_in' %></li>
                <li><%= link_to 'Sign Up!', '/users/sign_up' %></li>
              <%end%>

              </ul>
            </li>
          </ul>
        <form class="navbar-form navbar-left" role="search">
          <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
          </div>
          <button type="submit" class="btn btn-default">Submit</button>
        </form>
  
        
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->

  </nav>

  <div class="container">
    <h5><%= link_to '', "/" %></h5>
    <%= yield %>
    <h5><%= link_to 'New', "pills/new" %></h5>

</body>
</html>



